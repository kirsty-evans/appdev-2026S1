# Find Common

You have been tasked with writing a static method called `FindCommon` which will accept two integer arrays and find the overlap between the two.

The return value of the method is an `int[]` that will contain the common elements between the two.


```cs

static int[] FindCommon(int[] a, int[] b)
{

  return new int[1];
}

```






